---
description: No secrets in repo; mocked LLM in CI
alwaysApply: true
---

# Secrets and CI

- Never commit API keys, `.env`, or credentials
- Default tests mock LLM responses; no `OPENAI_API_KEY` required in CI
- Live tests only behind `QAPITOL_EVALS_LIVE=1` and `@pytest.mark.live`
